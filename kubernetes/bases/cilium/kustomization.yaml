---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Note: Cilium deployed via Helmfile bootstrap, not GitOps
# This kustomization only manages supplementary Cilium resources
resources: []
