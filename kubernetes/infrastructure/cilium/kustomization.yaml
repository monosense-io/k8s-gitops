---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ocirepository.yaml
  # Note: Deploy helmrelease-infra.yaml on infra cluster
  # Note: Deploy helmrelease-apps.yaml on apps cluster
  # These will be referenced from cluster-specific kustomizations
