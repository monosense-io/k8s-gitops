---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ocirepository.yaml
  # Cluster-specific Cilium HelmRelease (referenced conditionally via kustomization patches)
  - helmrelease-${CLUSTER}.yaml
