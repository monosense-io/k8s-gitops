---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ocirepository.yaml
  # Note: Cilium core (DaemonSet/Operator) is installed via bootstrap Helmfile
  # GitOps manages only day-2 features under networking/cilium/ (via its ks.yaml)
