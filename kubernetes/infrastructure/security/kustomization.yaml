---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Flux Kustomization resources for security components
  - rbac/ks.yaml
  - external-secrets/ks.yaml
  - cert-manager/ks.yaml
  # Component directories (contain actual manifests)
  - rbac  # Security BLOCKER #1 - RBAC Model (Added: 2025-10-14)
  - external-secrets
  - cert-manager
