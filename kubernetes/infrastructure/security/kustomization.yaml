---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Note: RBAC and cert-manager disabled for bootstrap
# Note: external-secrets enabled - required for ClusterSecretStore (needed by flux-instance)
resources:
  # Component directories (contain actual manifests)
  # - rbac  # Day 2 - RoleBindings reference monitoring, databases, rook-ceph namespaces
  - external-secrets  # Required for ClusterSecretStore
  # - cert-manager  # Day 2 - cert-manager already deployed via Helmfile
