---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - security/
  - storage/
  # Note: networking/ components wired directly in clusters/<cluster>/infrastructure.yaml
  # Note: gitops/ components wired separately if needed
