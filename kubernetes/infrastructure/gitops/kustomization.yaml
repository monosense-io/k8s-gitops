---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  # Component directories (contain actual manifests)
  # Note: Flux is bootstrapped via Helmfile; these are kept for reference only
  - flux-operator
  - flux-instance
