---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Cilium: Bootstrap via Helmfile, then Flux adopts via HelmRelease (buroa pattern)
  - cilium/core/ks.yaml
  - cilium/gateway/ks.yaml
  - cilium/bgp/
  - cilium/clustermesh/ks.yaml
  # Note: cilium/ipam excluded - has both infra/apps configs, handled per-cluster
  - coredns/ks.yaml
  - spegel/ks.yaml
  - cloudflared/ks.yaml
  - external-dns/
