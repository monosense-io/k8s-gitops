---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Note: Cilium Gateway and day-2 features disabled for bootstrap (need Gateway API CRDs)
# Note: Spegel disabled (Talos incompatibility)
resources:
  # Flux Kustomization resources for networking components
  # - cilium/ks.yaml  # Day 2 - needs Gateway API CRDs
  # - coredns/ks.yaml  # Day 2 - already deployed via Helmfile
  # - spegel/ks.yaml  # Day 2 - Talos incompatibility
  # Component directories (contain actual manifests)
  # - cilium  # Day 2 - Gateway API features
  # - coredns  # Day 2 - already deployed via Helmfile
  # - spegel  # Day 2 - Talos incompatibility
