---
# Network Policy - Default Deny All
# Denies all ingress and egress traffic
# Apply additional allow policies as needed
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
