---
# Service Monitor for Prometheus
# Automatically configured via Kustomize replacements
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: placeholder  # Replaced by Service name
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: placeholder  # Replaced by Service label
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
      scheme: http
