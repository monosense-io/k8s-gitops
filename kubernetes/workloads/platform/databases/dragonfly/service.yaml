---
apiVersion: v1
kind: Service
metadata:
  name: dragonfly
  namespace: dragonfly-system
  annotations:
    service.cilium.io/global: "true"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dragonfly
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
