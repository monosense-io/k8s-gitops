---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # CloudNative-PG Operator (REQUIRED - manages all CNPG resources)
  - ../../../../bases/cloudnative-pg/operator

  # Shared multi-tenant PostgreSQL cluster
  - shared-cluster

  # PgBouncer connection poolers
  - poolers
