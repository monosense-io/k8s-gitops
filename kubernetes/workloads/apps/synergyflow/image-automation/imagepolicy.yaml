apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: synergyflow-backend
  namespace: synergyflow
spec:
  imageRepositoryRef:
    name: synergyflow-backend
  policy:
    semver:
      range: ">=0.1.0"

