apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: synergyflow-backend
  namespace: synergyflow
spec:
  image: harbor.monosense.io/synergyflow/backend
  interval: 1m
  secretRef:
    name: harbor-registry-secret

