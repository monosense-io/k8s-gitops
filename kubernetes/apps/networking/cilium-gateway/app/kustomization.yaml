---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: networking
resources:
  # Cilium Gateway API controller is built into Cilium operator
  # No additional deployment needed - just configuration
