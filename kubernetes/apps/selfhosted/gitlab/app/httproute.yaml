---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gitlab
  namespace: selfhosted
spec:
  parentRefs:
    - name: cilium-external
      kind: Gateway
      namespace: networking
  hostnames:
    - git.monosense.dev
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: gitlab-webservice-default
          port: 8181
          weight: 100
---
# HTTPRoute for Container Registry (when enabled)
# Uncomment when registry is enabled in HelmRelease
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: gitlab-registry
#   namespace: selfhosted
# spec:
#   parentRefs:
#     - name: cilium-external
#       kind: Gateway
#       namespace: networking
#   hostnames:
#     - registry.monosense.dev
#   rules:
#     - matches:
#         - path:
#             type: PathPrefix
#             value: /
#       backendRefs:
#         - name: gitlab-registry
#           port: 5000
#           weight: 100
